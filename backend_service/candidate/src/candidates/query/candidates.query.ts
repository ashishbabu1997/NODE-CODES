export default {
    list:'SELECT c.candidate_name as "candidateName" ,p.company_name as "companyName",s.position_name as "positionName",c.description as "description",c.cover_note as "coverNote",c.resume as "resume",c.rate as "rate",c.phone_number as "phoneNumber",c.label as "label",c.email_address as "emailAddress",c.status as "status",c.candidate_status as "candidateStatus",c.job_received_id as "jobReceivedId"  FROM candidate c INNER JOIN company p ON p.company_id=c.company_id INNER JOIN positions s ON c.position_id=s.position_id WHERE c.candidate_id=$1 ',
    listCandidates:'SELECT c.candidate_id as "candidateId",c.candidate_name as "candidateName" ,p.company_name as "companyName",s.position_name as "positionName",c.status as "status",c.candidate_status as "candidateStatus" FROM candidate c INNER JOIN company p ON p.company_id=c.company_id INNER JOIN positions s ON c.position_id=s.position_id WHERE c.position_id=$1 '
   
}