export default {
    modifySocialProfileAndStatusUpdate:'insert into candidate_social(candidate_id, github, github_link, linkedin, linkedin_link, stackoverflow, stackoverflow_link,status, created_by, updated_by, created_on, updated_on) values($1,$2,$3,$4,$5,$6,$7,true,$8,$8,$9,$9) on conflict(candidate_id) do update set github=$2, github_link=$3, linkedin=$4, linkedin_link=$5, stackoverflow=$6,stackoverflow_link=$7,status=true,updated_by=$8,updated_on=$9;',
    addDefaultAssessmentTraits: 'INSERT into candidate_assesement (assesement_trait, candidate_id, assesement_type,is_link_available, created_by, updated_by, created_on, updated_on) values (unnest(array(select (review_name) from review_steps where status = true)), $1, unnest(array(select (review_type) from review_steps where status = true)), unnest(array(select (r.is_link_available) from review_steps r where status = true)), $2, $2, $3, $3) on conflict on constraint candidate_assesement_candidate_id_assesment_trait_unique_key do nothing',
    addSkillBasedAssesmentTraits : 'INSERT into candidate_assesement (assesement_trait, skill_id, candidate_id, assesement_type, created_by, updated_by, created_on, updated_on) values (unnest(array(select skill_name from candidate_skill cs join skills s on cs.skill_id = s.skill_id where candidate_id = $1 and (cs.competency = 2 or cs.competency = 3) and s.status = true)), unnest(array(select s.skill_id from candidate_skill cs join skills s on cs.skill_id = s.skill_id where candidate_id = $1 and (cs.competency = 2 or cs.competency = 3) and s.status = true)), $1, 4, $2, $2, $3, $3) on conflict on constraint candidate_assesement_candidate_id_assesment_trait_unique_key do nothing',
    listFreelancerJobs:'select p.position_id, position_name, developer_count, job_description, experience_level, jc.job_category_id,jc.job_category_name, array_agg (skill_name) as skills from positions p left join job_category jc on jc.job_category_id = p.job_category_id left join job_skills js on p.position_id = js.position_id and js.top_rated_skill=true and js.status=true left join skills s on js.skill_id = s.skill_id where p.status = true and p.job_status = 6 and p.position_name ilike $search group by p.position_id,jc.job_category_id ',
    updateCandidateStatus:'UPDATE candidate SET candidate_status=3,updated_by=$2,updated_on=$3 WHERE candidate_id=$1'
}